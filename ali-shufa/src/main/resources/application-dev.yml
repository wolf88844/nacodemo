server:
  port: 9999

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/shufa?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: root
  application:
    name: shufayun
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
  redis:
    database: 0
    host: localhost
    port: 6379
    password: founder
    timeout: 3000
logging:
  config: classpath:log4j2-dev.xml
